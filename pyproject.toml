[project]
name = "md-creator"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "python-multipart>=0.0.12",
    "markitdown[pdf,docx,pptx,xlsx,xls,doc,ppt]>=0.1.6",
]

[tool.vercel]
entrypoint = "api/index.py"
